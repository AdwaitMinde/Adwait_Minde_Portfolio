# This app does not call any external API today and needs no secrets to run.
# If you add one later (e.g. a live call to the Claude API), put the real
# value in .streamlit/secrets.toml (never commit that file) and read it with:
#
#   st.secrets["SOME_KEY"]
